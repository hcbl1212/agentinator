## What

<!-- One or two sentences: what does this change do? -->

## Try this

<!-- The 2–4 steps a reviewer follows to see it working in the running app. -->

1.

## Checklist

- [ ] `npm test` passes with 100% coverage (no unexplained `v8 ignore` comments)
- [ ] `npm run lint`, `npm run format:check`, and `npm run typecheck` pass
- [ ] `npm run dev` launches and the change is visible/working
